nameOverride: orders-service

replicaCount: 2

image:
  repository: ghcr.io/ivanribin/orders-service

service:
  port: 3000
  targetPort: 3000

envFromSecret: lab-secrets

env:
  - name: PORT
    value: "3000"

migrations:
  enabled: true
  command: ["node", "src/migrate.js"]
